// imaginarium-app — Phase 6 Slint native UI (not in default workspace).
fn main() {
    eprintln!(
        "imaginarium-app: Phase 6 not built yet. Use `imaginarium` CLI / future browser UI."
    );
    std::process::exit(1);
}
